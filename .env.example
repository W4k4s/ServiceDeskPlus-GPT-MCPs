# Copie este archivo a .env y complete los valores necesarios
# Obtenga la API Key en SDP desde su perfil de técnico (Generate API Key)
SDP_API_KEY=
# URL base de su instancia, por ejemplo https://mi-servidor:8443
SDP_BASE_URL=
